{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm --filter @ash/database prisma generate && pnpm --filter @ash/admin build",
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "outputDirectory": ".next",
  "regions": ["sin1"],
  "build": {
    "env": {
      "HUSKY": "0"
    }
  }
}

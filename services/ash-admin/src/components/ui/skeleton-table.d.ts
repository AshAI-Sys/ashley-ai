import React from "react";
interface SkeletonTableProps {
    rows?: number;
    columns?: number;
}
export declare function SkeletonTable({ rows, columns }: SkeletonTableProps): React.JSX.Element;
export default SkeletonTable;

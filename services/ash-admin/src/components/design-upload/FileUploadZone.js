'use client'

import React from 'react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function DesignUploadComponent() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Design Upload Component</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This design upload component is under development.</p>
      </CardContent>
    </Card>
  )
}

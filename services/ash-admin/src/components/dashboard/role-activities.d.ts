import { User } from '../../lib/permissions';
interface RoleActivitiesProps {
    user: User;
}
export default function RoleActivities({ user }: RoleActivitiesProps): import("react").JSX.Element;
export {};

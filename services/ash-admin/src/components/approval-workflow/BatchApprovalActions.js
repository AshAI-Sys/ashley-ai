'use client'

import React from 'react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'

export default function ApprovalComponent() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Approval Workflow Component</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This approval workflow component is under development.</p>
      </CardContent>
    </Card>
  )
}

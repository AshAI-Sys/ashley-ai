{
  "buildCommand": "npm install -g pnpm@9 && pnpm install --frozen-lockfile && cd packages/database && npx prisma generate && cd ../services/ash-admin && pnpm build",
  "installCommand": "echo 'Skipping npm install'",
  "framework": "nextjs",
  "outputDirectory": "services/ash-admin/.next"
}
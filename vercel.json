{
  "buildCommand": "pnpm install && cd packages/database && npx prisma generate && cd ../services/ash-admin && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": "services/ash-admin/.next"
}
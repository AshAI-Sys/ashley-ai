{
  "buildCommand": "cd ../../packages/database && npx prisma generate && cd ../services/ash-admin && pnpm build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}